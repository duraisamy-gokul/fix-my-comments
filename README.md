# Fix My Comments

AI-powered task management and collaboration layer attached directly to source code.

## Development

Install dependencies:

```bash
npm install
```

Compile:

```bash
npm run compile
```

Run the extension:

1. Open this repository in VS Code.
2. Press `F5`.
3. Run the `Fix My Comments: Hello World` command from the command palette.
