# Regenerate committed types when a spec exists, then keep them staged.
# Skipped until the first types/*.yaml is introduced (Phase 1), so commits
# are not blocked during the planning stage.
if ls types/*.yaml >/dev/null 2>&1; then
  npm run gen:types
  git add src/generated
fi

npm run check
